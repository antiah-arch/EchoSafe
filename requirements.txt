# EchoSafe — Python dependencies
# Install with:  pip install -r requirements.txt
#
# Requires Python 3.10+ (uses match/case, X | Y type unions)
# Tested on Python 3.12

# ── Core ML & audio ───────────────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
tensorflow>=2.13          # for CNN spectrogram classifier and TFLite inference
librosa>=0.10             # audio loading, mel spectrograms, pitch shift
soundfile>=0.12           # wav read/write (recognizer.py, sounds_review.py)
sounddevice>=0.4.6        # PC microphone input (recognizer.py, sounds_review.py)

# ── Serial ────────────────────────────────────────────────────────────────────
pyserial>=3.5             # Arduino serial communication

# ── Console output ────────────────────────────────────────────────────────────
colorama>=0.4.6           # cross-platform coloured terminal output (utils.py)

# ── Optional — dataset download only ─────────────────────────────────────────
# These are only needed if you run dataset_downloader.py to fetch ESC-50.
# They are part of the standard library (tarfile, urllib) except for the above.
# No additional packages required for dataset_downloader.py.